import React from 'react';
import Layout from '../../Layout';

const NotFound = () => (
  <Layout>
    <div className={'NotFound'}>
      <h1>
        That is a 404.
      </h1>
    </div>
  </Layout>
);

export default NotFound;
