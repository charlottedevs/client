import React from 'react';
import Layout from '../../Layout';

const Home = () => (
  <Layout>
    <div className={'home'}>
      <h1>
        This is home
      </h1>
    </div>
  </Layout>
);

export default Home;
